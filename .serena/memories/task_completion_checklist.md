# Task Completion Checklist

After completing a task, run the following:

1. **Build:** `bun run build` — ensure all packages compile
2. **Type-check:** `bun run type-check` — ensure no type errors
3. No tests, linters, or formatters are currently configured
