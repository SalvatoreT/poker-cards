{
	"name": "example",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun build index.html --outdir dist --minify",
		"preview": "bun run serve.ts"
	},
	"dependencies": {
		"poker-cards": "workspace:*",
		"poker-card-element": "workspace:*"
	}
}
